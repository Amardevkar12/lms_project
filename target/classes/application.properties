spring.application.name=leave-management-system

# ================= DATABASE (ONLY RAILWAY) =================
spring.datasource.url=jdbc:mysql://tramway.proxy.rlwy.net:29929/railway
spring.datasource.username=root
spring.datasource.password=PHOFxsxAKpQPiPnEDQKOaxNeLjgYZngL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ================= JPA =================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ================= SERVER =================
server.port=${PORT:8080}

# ================= JWT =================
jwt.secret=lms_super_secret_key_that_is_at_least_256_bits_long_for_hs256_algorithm_2024
jwt.expiration=86400000

# ================= EMAIL =================
app.mail.enabled=${APP_MAIL_ENABLED:true}
app.mail.from-address=${MAIL_USERNAME:}
app.mail.from-name=Leave Management System

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ================= LOGGING =================
logging.level.com.lms=DEBUG
logging.level.org.springframework.security=DEBUG
